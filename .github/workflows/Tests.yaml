name: Tests

on:
  push:
  workflow_dispatch:

jobs:
  tests:
    name: "Run Tests"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Run docker compose
        run: docker-compose up -d
      - name: Run tests
        run: . scripts/pytest
